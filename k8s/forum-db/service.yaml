apiVersion: v1
kind: Service
metadata:
  name: forum-db-service
  namespace: mental-health-forum
spec:
  selector:
    app: forum-db
  ports:
  - port: 5432
    targetPort: 5432
  clusterIP: None  # Headless service for StatefulSet
